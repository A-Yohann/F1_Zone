{% extends 'base.html.twig' %}

{% block title %}Pilotes F1{% endblock %}

{% block body %}
<div class="container">
<section>
    <h1 class="pilotes-title">Tous les pilotes</h1>
    <div class="pilotes-search-bar" style="margin-bottom:1.5rem;text-align:center;">
        <input type="text" id="pilote-search" placeholder="Rechercher un pilote..." style="padding:0.5rem 1rem; width:100%; max-width:350px; border-radius:8px; border:1px solid #ccc; font-size:1.1rem;">
    </div>
    <div class="pilotes-cards">
        {% for pilote in pilotes %}
            <div class="pilote-card">
                <img src="{{ asset('assets/img/pilote/' ~ (pilote.photo ?: '../divers/pilotes-grp.webp')) }}" alt="{{ pilote.prenom }} {{ pilote.nom }}">
                <div class="pilote-info">
                    <h3>{{ pilote.prenom }} {{ pilote.nom }}</h3>
                    <div class="ecurie">{{ pilote.ecurie ? pilote.ecurie.nom : 'Sans écurie' }}</div>
                    <div class="numero">#{{ pilote.numero ?: '?' }}</div>
                    <a href="{{ path('app_pilote_show', {id: pilote.id}) }}" class="profil-btn btn">Voir profil</a>
                </div>
            </div>
        {% else %}
            <p>Aucun pilote trouvé.</p>
        {% endfor %}
    </div>
</section>
</div>
{% endblock %}
